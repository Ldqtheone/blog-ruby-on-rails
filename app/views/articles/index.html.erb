<h1 class="avlisTitle" xmlns="http://www.w3.org/1999/html">Avlis-Formation : Articles et Avis</h1>
<p>Notre page possède actuellement <%= Article.public_count %> articles!</p>

<div class="row">
  <div class="articlesGroupe">

    <% @articles.each do |article| %>
      <% unless article.archived? %>

        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= article.title %></h5>
            <p class="card-text"><%= article.body %></p>
            <%= link_to "Voir l'article", article, :class => "viewArticle" %>
          </div>
        </div>

      <% end %>
    <% end %>

    <button class="btn btn-primary newArticleLink" type="submit">
      <%= link_to "Ecrire un nouvel article", new_article_path %>
    </button>

  </div>
</div>



